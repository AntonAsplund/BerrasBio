@model BerrasBio.ViewModels.CreateViewingViewModel

@{
    ViewData["Title"] = "Create";
}

@if(TempData["Message"] != null)
{
    <div class="alert alert-info">
        @TempData["Message"];
    </div>
}

<h1>Create viewing</h1>

<div>
    <form method="post">

        @*<select name="movieId" asp-items="@(new SelectList(Model.Movies, "MovieId", "Title"))">
            <option>Select a movie...</option>
        </select>*@

        <select name="salonId" asp-items="@(new SelectList(Model.Salons, "SalonId", "Name"))">
            <option>Select a salon...</option>
        </select>

        <div class="form-group">
            <label class="control-label">Start Time:</label>
            <input type="datetime-local" name="startTime" class="form-control" />
        </div>

        <div class="form-group">
            <input type="submit" value="Create" class="btn btn-primary" />
        </div>

    </form>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

