1. Öppna projektet och kör 'publish' på BerrasDb, och välj din server och namn på databasen, så databasen genereras. (Då kommer även testdata genereras automatiskt så kör INTE Script.Postdeployment skripterna igen).
2. Ändra 'BerrasConnectionString' så den matchar den genererade databasen.
3. Kör websidan och logga in med användarnamnet 'Claes' med lösenordet 'password'.